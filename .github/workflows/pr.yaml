name: pr

on: [pull_request]

jobs:
  hadolint:
    uses: wittdennis/pipelines/.github/workflows/hadolint.yaml@cfd75389228d28dcd8aa4cc4d27599426ff9746c # 2.0.18

  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@cfd75389228d28dcd8aa4cc4d27599426ff9746c # 2.0.18
    needs:
      - hadolint
    with:
      image-name: atlantis
      create-artifacts: false
