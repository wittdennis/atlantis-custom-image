name: pr

on: [pull_request]

jobs:
  hadolint:
    uses: wittdennis/pipelines/.github/workflows/hadolint.yaml@ef870f2155acbf0282ee4b81501d3682ba2048d2 # 2.0.10

  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@ef870f2155acbf0282ee4b81501d3682ba2048d2 # 2.0.10
    needs:
      - hadolint
    with:
      image-name: atlantis
      create-artifacts: false
