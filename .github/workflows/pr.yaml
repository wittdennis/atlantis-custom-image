name: pr

on: [pull_request]

jobs:
  hadolint:
    uses: wittdennis/pipelines/.github/workflows/hadolint.yaml@6b71882a90b71468a66605f934ef75a83bbe006b # 2.0.25

  build:
    uses: wittdennis/pipelines/.github/workflows/container-image-build.yaml@6b71882a90b71468a66605f934ef75a83bbe006b # 2.0.25
    needs:
      - hadolint
    with:
      image-name: atlantis
      create-artifacts: false
